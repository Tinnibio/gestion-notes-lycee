services:
  - type: web
    name: gestion-notes-lycee
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: gestion-notes-db
          property: connectionString

databases:
  - name: gestion-notes-db
    databaseName: gestion_notes_lycee
    plan: free